# 下一步操作指南

## 📋 操作检查清单

### ✅ 已完成的工作

- [x] 配置 `_config.yml` 启用 Giscus 评论系统
- [x] 配置 `_config.yml` 启用不蒜子浏览量统计
- [x] 创建 Jekyll 插件自动注入不蒜子统计脚本
- [x] 创建配置文档和快速开始指南

### 🔄 需要您完成的操作

#### 第一步：启用 GitHub Discussions（必需）

1. **访问仓库设置页面**
   ```
   https://github.com/kevinchen202008-cmyk/kevinchen202008-cmyk.github.io/settings
   ```

2. **启用 Discussions 功能**
   - 滚动到页面底部的 **Features** 部分
   - 找到 **Discussions** 选项
   - 勾选复选框
   - 点击 **Save changes** 按钮

3. **验证启用成功**
   - 刷新页面后，应该能看到仓库顶部导航栏出现 **Discussions** 标签

#### 第二步：获取 Giscus 配置参数（必需）

1. **访问 Giscus 配置页面**
   ```
   https://giscus.app/zh-CN
   ```

2. **填写仓库信息**
   - **Repository**: `kevinchen202008-cmyk/kevinchen202008-cmyk.github.io`
   - **Discussion category**: 选择 `Announcements` 或 `General`
   - **Page ↔ Discussions mapping**: 选择 `pathname`
   - **Discussion term**: 选择 `pathname`
   - **Theme**: 选择 `Preferred color scheme`（跟随系统主题）
   - **Language**: 选择 `zh-CN`
   - **Reactions enabled**: 保持勾选 ✅

3. **获取配置参数**
   - 点击页面底部的 **Generate script** 按钮
   - 在生成的配置中找到以下参数：
     - `data-repo-id` → 对应 `repo_id`
     - `data-category` → 对应 `category`
     - `data-category-id` → 对应 `category_id`

4. **更新 `_config.yml`**
   - 打开项目中的 `_config.yml` 文件
   - 找到第 131-140 行的 `giscus` 配置部分
   - 填入获取的参数：
     ```yaml
     giscus:
       repo: kevinchen202008-cmyk/kevinchen202008-cmyk.github.io
       repo_id: "你的repo_id"  # 从 giscus.app 获取
       category: "Announcements"  # 从 giscus.app 获取
       category_id: "你的category_id"  # 从 giscus.app 获取
       mapping: pathname
       strict: "0"
       input_position: bottom
       lang: zh-CN
       reactions_enabled: "1"
     ```

#### 第三步：提交并推送代码

1. **检查更改**
   ```bash
   git status
   ```

2. **添加更改的文件**
   ```bash
   git add _config.yml
   git add _plugins/
   git add _includes/
   git add assets/
   git add *.md
   ```

3. **提交更改**
   ```bash
   git commit -m "feat: 添加评论系统和浏览量统计功能

   - 配置 Giscus 评论系统（基于 GitHub Discussions）
   - 实现不蒜子浏览量统计
   - 添加 Jekyll 插件自动注入统计脚本
   - 添加配置文档和快速开始指南"
   ```

4. **推送到 GitHub**
   ```bash
   git push origin main
   # 或者 git push origin master（根据你的默认分支）
   ```

#### 第四步：等待部署并验证

1. **等待 GitHub Pages 构建**
   - 访问：https://github.com/kevinchen202008-cmyk/kevinchen202008-cmyk.github.io/actions
   - 查看最新的构建状态
   - 通常需要 1-2 分钟完成构建

2. **验证评论功能**
   - 访问任意文章页面，例如：
     ```
     https://kevinchen202008-cmyk.github.io/posts/my-first-blog/
     ```
   - 滚动到页面底部
   - 应该能看到 Giscus 评论框
   - 使用 GitHub 账号登录后可以发表评论

3. **验证浏览量统计**
   - 刷新页面几次
   - 等待 2-3 分钟
   - 浏览量数字应该会更新（如果主题支持显示）

## 🔍 故障排查

### 评论框不显示？

**检查清单：**
1. ✅ GitHub Discussions 是否已启用？
2. ✅ `_config.yml` 中的 `comments.provider` 是否为 `giscus`？
3. ✅ Giscus 配置参数是否完整（repo_id、category_id 等）？
4. ✅ GitHub Pages 构建是否成功？
5. ✅ 浏览器控制台是否有错误信息？（按 F12 查看）

**解决方案：**
- 如果参数缺失，重新访问 giscus.app 获取完整配置
- 如果构建失败，检查 `_config.yml` 的 YAML 语法是否正确
- 清除浏览器缓存后重试

### 浏览量不显示？

**检查清单：**
1. ✅ `_config.yml` 中的 `pageviews.provider` 是否为 `busuanzi`？
2. ✅ Jekyll 插件是否正常工作？
3. ✅ 等待足够时间让统计数据加载（通常需要几分钟）

**解决方案：**
- 检查浏览器控制台的网络请求，确认不蒜子脚本已加载
- 查看页面源代码，确认 `<script>` 标签已注入
- 如果主题不支持自动显示，可能需要手动添加显示位置

## 📝 可选：自定义显示位置

如果 Chirpy 主题没有自动显示浏览量，您可以：

1. **在文章页面显示浏览量**
   - 编辑文章模板或创建自定义布局
   - 在文章元数据区域添加：
     ```html
     {% include busuanzi-pageviews.html %}
     ```

2. **在页脚显示站点访问量**
   - 创建或编辑 `_includes/footer.html`
   - 添加：
     ```html
     {% include busuanzi-siteviews.html %}
     ```

## 🎯 完成标准

当以下所有项都完成时，功能即配置成功：

- [ ] GitHub Discussions 已启用
- [ ] Giscus 配置参数已填入 `_config.yml`
- [ ] 代码已提交并推送到 GitHub
- [ ] GitHub Pages 构建成功
- [ ] 文章页面底部显示评论框
- [ ] 可以成功发表评论
- [ ] 可以回复评论（嵌套回复）
- [ ] 浏览量统计正常工作（如果主题支持显示）

## 📚 参考文档

- [Giscus 官方文档](https://giscus.app/zh-CN)
- [不蒜子统计官网](https://busuanzi.ibruce.info/)
- [Chirpy 主题文档](https://github.com/cotes2020/jekyll-theme-chirpy/wiki)
- [快速开始指南](./QUICK_START.md)
- [详细配置文档](./COMMENTS_AND_VIEWS_SETUP.md)

## 💡 提示

- Giscus 配置参数只需要获取一次，之后可以重复使用
- 不蒜子统计是自动的，无需额外配置
- 如果遇到问题，先检查 GitHub Pages 的构建日志
- 建议在本地先测试（`bundle exec jekyll serve`）再推送到 GitHub
